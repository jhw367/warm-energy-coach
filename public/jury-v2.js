const state={ready:7,reserve:35,weather:'sunny',storyStep:0};
const scenarios={
  sunny:{label:'Sunny tomorrow',solar:36,cloud:0,price:0.29},
  cloudy:{label:'Cloudy tomorrow',solar:15,cloud:1,price:0.30},
  expensive:{label:'Expensive evening',solar:29,cloud:0,price:0.42}
};
const q=s=>document.querySelector(s);
const qa=s=>[...document.querySelectorAll(s)];
const two=n=>String(n).padStart(2,'0');
function model(){const s=scenarios[state.weather];const reservePenalty=Math.max(0,state.reserve-35)*0.045;const lateBonus=state.ready>=7?1.15:0.65;const solarCapture=Math.max(3,Math.round(s.solar*0.32-state.reserve*0.035));const chargeStart=s.cloud?23:13;const chargeKwh=s.cloud?30:34;const gridPeak=Math.max(2,Math.round(16-reservePenalty*5-solarCapture*0.25));const saving=Math.max(0.8,2.84+(s.price-0.29)*12+lateBonus-reservePenalty-(s.cloud?0.65:0));const selfUse=Math.min(88,61+solarCapture*0.65+(state.reserve-25)*0.12);const confidence=s.cloud?'Medium':'High';return {...s,chargeStart,chargeKwh,gridPeak,saving,selfUse,solarCapture,confidence};}
function header(){return `<div class="topbar"><strong>SYNTHETIC JURY DEMO</strong><span>No private data &middot; no device control &middot; every recommendation is explainable</span></div><div class="shell"><header class="site-header"><div class="brand"><div class="brand-mark">W</div><div><strong>WARM</strong><small>the household energy coach</small></div></div><div class="journey" role="navigation" aria-label="Demo sections"><button aria-selected="true" data-jump="plan">Plan</button><button aria-selected="false" data-jump="why">Why</button><button aria-selected="false" data-jump="impact">Impact</button></div></header>`;}
function presentationLaunch(){return `<section class="present-launch" aria-label="Presentation mode"><div><strong>Tell the complete WARM story once.</strong><span>A fixed seven-step, 90-second narrative. The interactive demo remains available underneath as evidence.</span></div><button id="startPresentation" type="button">Start 90-second story</button></section>`;}
function hero(m){return `<section class="hero" id="plan"><article class="hero-copy"><p class="eyebrow">Tomorrow's coordinated plan</p><h1>Charge the car when the whole house benefits.</h1><p>WARM combines solar, tariff, battery reserve and departure time into one household decision instead of showing four separate dashboards.</p><div class="recommendation"><b>1</b><div><strong>Start charging at ${two(m.chargeStart)}:00 and keep ${state.reserve}% battery reserve.</strong><span>The car receives ${m.chargeKwh} kWh before ${two(state.ready)}:00, while the home avoids the expensive evening peak.</span></div></div></article><aside class="hero-card"><p class="eyebrow">Expected result</p><h2>One decision, four effects</h2><p>The figures update as the jury changes household constraints below.</p><div class="score-grid"><article class="score"><strong>&euro;${m.saving.toFixed(2)}</strong><span>modeled daily value</span></article><article class="score"><strong>${m.gridPeak} kWh</strong><span>peak-grid demand avoided</span></article><article class="score"><strong>${Math.round(m.selfUse)}%</strong><span>solar used at home</span></article></div></aside></section>`;}
function controls(){return `<section class="panel" id="interactive"><div class="panel-head"><div><p class="eyebrow">Change the household, not a chart</p><h2>What must tomorrow's plan respect?</h2></div><p>Every control immediately recalculates the recommendation.</p></div><div class="controls"><div class="control"><label for="ready">Car ready by</label><output id="readyOut">${two(state.ready)}:00</output><small>Departure deadline</small><input id="ready" type="range" min="5" max="10" value="${state.ready}"></div><div class="control"><label for="reserve">Battery reserve</label><output id="reserveOut">${state.reserve}%</output><small>Energy kept for the home</small><input id="reserve" type="range" min="20" max="60" step="5" value="${state.reserve}"></div><div class="control"><label>Tomorrow</label><small>Forecast and price scenario</small><div class="scenario-chips">${Object.entries(scenarios).map(([key,value])=>`<button type="button" data-weather="${key}" class="${state.weather===key?'active':''}">${value.label}</button>`).join('')}</div></div></div></section>`;}
function flow(m){return `<section class="story-grid" id="why"><article class="panel flow-panel"><div class="panel-head"><div><p class="eyebrow">A system of communicating vessels</p><h2>WARM coordinates every flow.</h2></div><p>Animated direction shows the selected plan.</p></div><div class="flow-map"><div class="node solar"><div class="icon">SUN</div><strong>Solar</strong><span>${m.solar} kWh forecast</span></div><div class="node battery"><div class="icon">BAT</div><strong>Battery</strong><span>${state.reserve}% reserve</span></div><div class="node home"><div class="icon">HOME</div><strong>Home</strong><span>Evening protected</span></div><div class="node car"><div class="icon">EV</div><strong>Car</strong><span>${m.chargeKwh} kWh needed</span></div><div class="node grid"><div class="icon">GRID</div><strong>Grid</strong><span>Peak avoided</span></div><div class="flow-line line-solar pulse"></div><div class="flow-line line-battery pulse"></div><div class="flow-line line-car pulse"></div><div class="flow-line line-grid pulse"></div></div></article><aside class="panel"><div class="panel-head"><div><p class="eyebrow">Explainable reasoning</p><h2>Why this plan?</h2></div></div><div class="reasoning"><article class="reason"><b>1</b><div><strong>Observe</strong><p>${m.solar} kWh solar forecast, &euro;${m.price.toFixed(2)}/kWh evening price and a ${state.reserve}% reserve requirement.</p></div></article><article class="reason"><b>2</b><div><strong>Respect constraints</strong><p>The car must be ready by ${two(state.ready)}:00 and the battery cannot fall below the chosen home reserve.</p></div></article><article class="reason"><b>3</b><div><strong>Reject weaker options</strong><p>Immediate charging increases grid demand; draining the battery leaves the home exposed during the evening.</p></div></article><article class="reason"><b>4</b><div><strong>Recommend</strong><p>Start at ${two(m.chargeStart)}:00. Confidence: ${m.confidence}. Expected value: &euro;${m.saving.toFixed(2)}.</p></div></article></div></aside></section>`;}
function timeline(m){const bars=Array.from({length:24},(_,hour)=>{let height=18;if(hour>6&&hour<19)height+=Math.round(Math.sin((hour-6)/12*Math.PI)*65*(m.solar/36));const charging=(m.chargeStart===13&&hour>=13&&hour<=16)||(m.chargeStart===23&&(hour===23||hour<4));if(charging)height=90;const cls=charging?'charge':(hour>7&&hour<18?'solar':'price');return `<div class="bar ${cls}" style="height:${Math.max(12,height)}px"><span>${hour%3===0?two(hour):''}</span></div>`;}).join('');return `<section class="panel"><div class="panel-head"><div><p class="eyebrow">24-hour plan</p><h2>Timing becomes visible.</h2></div><p>Gold = solar &middot; green = planned EV charging &middot; grey = other hours</p></div><div class="timeline">${bars}</div><div class="legend"><span><i style="background:#efc45f"></i>solar window</span><span><i style="background:#1f765d"></i>coordinated charging</span><span><i style="background:#cdd8d3"></i>household baseline</span></div></section>`;}
function impact(m){return `<section class="panel" id="impact"><div class="panel-head"><div><p class="eyebrow">Before and after</p><h2>From disconnected devices to one household plan.</h2></div><p>Alternative outcomes are kept separate and transparent.</p></div><div class="compare"><article class="compare-card"><h3>Without coordination</h3><div class="compare-row"><span>EV charging</span><strong>Immediate</strong></div><div class="compare-row"><span>Battery</span><strong>May drain early</strong></div><div class="compare-row"><span>Solar export</span><strong>${Math.round(18-m.solarCapture/3)} kWh</strong></div><div class="compare-row"><span>Decision</span><strong>Four apps</strong></div></article><article class="compare-card good"><h3>With WARM</h3><div class="compare-row"><span>EV charging</span><strong>${two(m.chargeStart)}:00</strong></div><div class="compare-row"><span>Battery</span><strong>${state.reserve}% protected</strong></div><div class="compare-row"><span>Solar captured</span><strong>+${m.solarCapture} kWh</strong></div><div class="compare-row"><span>Decision</span><strong>One plan</strong></div></article></div><div class="impact"><article><strong>&euro;${m.saving.toFixed(2)}</strong><span>daily modeled value</span></article><article><strong>${m.gridPeak} kWh</strong><span>peak import avoided</span></article><article><strong>${Math.round(m.selfUse)}%</strong><span>self-use</span></article><article><strong>${m.confidence}</strong><span>recommendation confidence</span></article></div></section><details class="details"><summary>Evidence, assumptions and safety</summary><div>All household values are synthetic. The demo sends no device commands and stores no changes. The recommendation uses a bounded model combining a 24-hour solar profile, tariff scenario, EV energy need, departure deadline and minimum home-battery reserve. Monetary impact is illustrative, not a quote. Every result is designed to show what information changed the recommendation.</div></details>`;}
function storyData(m){return [
{kicker:'1. The problem',title:'Every device has an app. The household still has no plan.',script:'Most energy dashboards look backwards. Solar, battery, car and tariff each optimize themselves. <strong>The resident is left to combine four separate answers.</strong>',cue:'Read this as the problem statement',visual:`<div class="story-problem"><article class="story-app"><b>SOLAR</b><span>Shows production</span></article><article class="story-app"><b>BATTERY</b><span>Shows state of charge</span></article><article class="story-app"><b>EV</b><span>Shows charging</span></article><article class="story-app"><b>PRICE</b><span>Shows tariffs</span></article></div>`},
{kicker:'2. Tomorrow',title:'One ordinary day creates a coordination problem.',script:`Tomorrow the car needs <strong>${m.chargeKwh} kWh by ${two(state.ready)}:00</strong>. Solar peaks in the afternoon. The evening price is &euro;${m.price.toFixed(2)} per kWh. The home must keep <strong>${state.reserve}% battery reserve</strong>.`,cue:'The case is concrete and understandable',visual:`<div class="story-case"><div class="story-fact"><span>Car ready</span><strong>${two(state.ready)}:00</strong></div><div class="story-fact"><span>Energy needed</span><strong>${m.chargeKwh} kWh</strong></div><div class="story-fact"><span>Solar forecast</span><strong>${m.solar} kWh</strong></div><div class="story-fact"><span>Home reserve</span><strong>${state.reserve}%</strong></div></div>`},
{kicker:'3. What WARM understands',title:'WARM treats the home as one connected energy system.',script:'It combines the forecast, tariff, normal home demand, battery limits and the car deadline. <strong>Every change in one part is evaluated against the others.</strong>',cue:'This is the differentiating capability',visual:`<div class="story-orbit"><div class="core">ONE PLAN</div><div class="sat s1">SOLAR</div><div class="sat s2">EV</div><div class="sat s3">TARIFF</div><div class="sat s4">BATTERY</div></div>`},
{kicker:'4. The decision',title:`Charge at ${two(m.chargeStart)}:00, protect the home, and finish before departure.`,script:`WARM schedules the car for the strongest household window, keeps the battery above ${state.reserve}%, and avoids unnecessary evening grid demand. <strong>One recommendation coordinates all devices.</strong>`,cue:'State the decision without technical detail',visual:`<div class="story-decision"><div class="decision-window"><span>Recommended charging window</span><strong>${two(m.chargeStart)}:00 - ${two((m.chargeStart+4)%24)}:00</strong></div><div class="decision-rule"><article><b>${m.chargeKwh} kWh</b><span>car ready</span></article><article><b>${state.reserve}%</b><span>home reserve</span></article><article><b>${m.confidence}</b><span>confidence</span></article></div></div>`},
{kicker:'5. Why this plan',title:'The recommendation is explainable, including rejected alternatives.',script:'Immediate charging would increase expensive grid demand. Draining the battery would expose the home in the evening. Waiting too long could miss the departure deadline. <strong>WARM chooses the strongest option that respects every constraint.</strong>',cue:'The system explains both choice and rejection',visual:`<div class="story-reasons"><article><b>1</b><div><strong>Reject immediate charging</strong><span>Too much grid demand</span></div></article><article><b>2</b><div><strong>Reject battery depletion</strong><span>Home reserve is protected</span></div></article><article><b>3</b><div><strong>Respect departure</strong><span>Car is ready on time</span></div></article></div>`},
{kicker:'6. The result',title:'The household gets a better outcome without manual planning.',script:`In this synthetic example, the plan captures ${m.solarCapture} kWh more solar, avoids ${m.gridPeak} kWh of peak-grid demand and creates about <strong>&euro;${m.saving.toFixed(2)} of modeled daily value</strong>. The car is ready and the home reserve remains protected.`,cue:'All figures are synthetic and clearly labeled',visual:`<div class="story-result"><article><strong>+${m.solarCapture} kWh</strong><span>solar captured</span></article><article><strong>${m.gridPeak} kWh</strong><span>peak import avoided</span></article><article><strong>&euro;${m.saving.toFixed(2)}</strong><span>modeled value</span></article><article><strong>${state.reserve}%</strong><span>reserve protected</span></article></div>`},
{kicker:'7. The core idea',title:'WARM does not just show what happened. It coordinates what should happen next.',script:'That is the product: <strong>one transparent household plan</strong>, created from connected energy flows, user priorities and tomorrow\'s conditions. The interactive demo underneath lets the jury test that promise.',cue:'Finish here, then open the live controls',visual:`<div class="story-core"><strong>From four dashboards to one household decision.</strong><span>Forward-looking. Explainable. Safe. User-controlled.</span></div>`}
];}
function presenter(m){const steps=storyData(m);const step=steps[state.storyStep];return `<section class="presenter" id="presenter" hidden aria-modal="true" role="dialog" aria-label="WARM 90-second presentation"><header class="presenter-top"><div class="presenter-brand"><b>W</b><div><strong>WARM in 90 seconds</strong><span>One linear story, then the interactive proof</span></div></div><button class="presenter-close" type="button" data-close>Close</button></header><div class="presenter-stage"><article class="presenter-copy"><p class="presenter-kicker">${step.kicker}</p><h2>${step.title}</h2><p class="presenter-script">${step.script}</p><span class="presenter-cue">Speaker cue: ${step.cue}</span></article><aside class="presenter-visual"><div class="story-visual">${step.visual}</div></aside></div><footer class="presenter-bottom"><div class="presenter-nav"><button class="presenter-prev" type="button" data-prev ${state.storyStep===0?'disabled':''}>Back</button><button class="presenter-next" type="button" data-next>${state.storyStep===steps.length-1?'Open live demo':'Next'}</button></div><div class="presenter-progress">${steps.map((_,index)=>`<button type="button" data-step="${index}" class="${index===state.storyStep?'active':''}" aria-label="Go to step ${index+1}"></button>`).join('')}</div><div class="presenter-count">${state.storyStep+1} / ${steps.length}</div></footer></section>`;}
function render(){const m=model();document.body.innerHTML=header()+presentationLaunch()+hero(m)+controls()+flow(m)+timeline(m)+impact(m)+`<footer class="footer"><span>WARM &middot; public jury experience</span><span>Read-only &middot; synthetic household &middot; transparent reasoning</span></footer></div>`+presenter(m);bind();}
function openPresenter(step=0){state.storyStep=step;render();const el=q('#presenter');el.hidden=false;document.body.style.overflow='hidden';q('.presenter-next')?.focus();}
function closePresenter(scrollToDemo=false){const el=q('#presenter');if(el)el.hidden=true;document.body.style.overflow='';if(scrollToDemo)q('#interactive')?.scrollIntoView({behavior:'smooth'});}
function refreshPresenter(){const current=q('#presenter');if(!current)return;const wrapper=document.createElement('div');wrapper.innerHTML=presenter(model());current.replaceWith(wrapper.firstElementChild);q('#presenter').hidden=false;bindPresenter();}
function bindPresenter(){q('[data-close]')?.addEventListener('click',()=>closePresenter(false));q('[data-prev]')?.addEventListener('click',()=>{if(state.storyStep>0){state.storyStep--;refreshPresenter();}});q('[data-next]')?.addEventListener('click',()=>{const last=storyData(model()).length-1;if(state.storyStep<last){state.storyStep++;refreshPresenter();}else closePresenter(true);});qa('[data-step]').forEach(button=>button.addEventListener('click',()=>{state.storyStep=Number(button.dataset.step);refreshPresenter();}));}
function bind(){q('#startPresentation')?.addEventListener('click',()=>openPresenter(0));q('#ready')?.addEventListener('input',event=>{state.ready=Number(event.target.value);render();});q('#reserve')?.addEventListener('input',event=>{state.reserve=Number(event.target.value);render();});qa('[data-weather]').forEach(button=>button.addEventListener('click',()=>{state.weather=button.dataset.weather;render();}));qa('[data-jump]').forEach(button=>button.addEventListener('click',()=>q('#'+button.dataset.jump)?.scrollIntoView({behavior:'smooth'})));bindPresenter();}
document.addEventListener('keydown',event=>{const presenterEl=q('#presenter');if(!presenterEl||presenterEl.hidden)return;if(event.key==='Escape')closePresenter(false);if(event.key==='ArrowRight')q('[data-next]')?.click();if(event.key==='ArrowLeft')q('[data-prev]')?.click();});
render();