const state={ready:7,reserve:35,weather:'sunny'};
const scenarios={sunny:{label:'Sunny tomorrow',solar:36,cloud:0,price:0.29},cloudy:{label:'Cloudy tomorrow',solar:15,cloud:1,price:0.30},expensive:{label:'Expensive evening',solar:29,cloud:0,price:0.42}};
const q=s=>document.querySelector(s);const qa=s=>[...document.querySelectorAll(s)];
function model(){const s=scenarios[state.weather];const reservePenalty=Math.max(0,state.reserve-35)*.045;const lateBonus=state.ready>=7?1.15:.65;const solarCapture=Math.max(3,Math.round(s.solar*.32-state.reserve*.035));const chargeStart=s.cloud?23:13;const chargeKwh=s.cloud?30:34;const gridPeak=Math.max(2,Math.round(16-reservePenalty*5-solarCapture*.25));const saving=Math.max(.8,2.84+(s.price-.29)*12+lateBonus-reservePenalty-(s.cloud?.65:0));const selfUse=Math.min(88,61+solarCapture*.65+(state.reserve-25)*.12);const confidence=s.cloud?'Medium':'High';return{...s,chargeStart,chargeKwh,gridPeak,saving,selfUse,solarCapture,confidence};}
function header(){return `<div class="topbar"><strong>SYNTHETIC JURY DEMO</strong><span>No private data · no device control · every recommendation is explainable</span></div><div class="shell"><header class="site-header"><div class="brand"><div class="brand-mark">W</div><div><strong>WARM</strong><small>the household energy coach</small></div></div><div class="journey" role="tablist"><button aria-selected="true" data-jump="plan">Plan</button><button aria-selected="false" data-jump="why">Why</button><button aria-selected="false" data-jump="impact">Impact</button></div></header>`;}
function hero(m){return `<section class="hero" id="plan"><article class="hero-copy"><p class="eyebrow">Tomorrow's coordinated plan</p><h1>Charge the car when the whole house benefits.</h1><p>WARM combines solar, tariff, battery reserve and departure time into one household decision instead of showing four separate dashboards.</p><div class="recommendation"><b>1</b><div><strong>Start charging at ${String(m.chargeStart).padStart(2,'0')}:00 and keep ${state.reserve}% battery reserve.</strong><span>The car receives ${m.chargeKwh} kWh before ${String(state.ready).padStart(2,'0')}:00, while the home avoids the expensive evening peak.</span></div></div></article><aside class="hero-card"><p class="eyebrow">Expected result</p><h2>One decision, four effects</h2><p>The figures update as the jury changes household constraints below.</p><div class="score-grid"><article class="score"><strong>€${m.saving.toFixed(2)}</strong><span>modeled daily value</span></article><article class="score"><strong>${m.gridPeak} kWh</strong><span>peak-grid demand avoided</span></article><article class="score"><strong>${Math.round(m.selfUse)}%</strong><span>solar used at home</span></article></div></aside></section>`;}
function controls(){return `<section class="panel"><div class="panel-head"><div><p class="eyebrow">Change the household, not a chart</p><h2>What must tomorrow's plan respect?</h2></div><p>Every control immediately recalculates the recommendation.</p></div><div class="controls"><div class="control"><label for="ready">Car ready by</label><output id="readyOut">${String(state.ready).padStart(2,'0')}:00</output><small>Departure deadline</small><input id="ready" type="range" min="5" max="10" value="${state.ready}"></div><div class="control"><label for="reserve">Battery reserve</label><output id="reserveOut">${state.reserve}%</output><small>Energy kept for the home</small><input id="reserve" type="range" min="20" max="60" step="5" value="${state.reserve}"></div><div class="control"><label>Tomorrow</label><small>Forecast and price scenario</small><div class="scenario-chips">${Object.entries(scenarios).map(([k,v])=>`<button data-weather="${k}" class="${state.weather===k?'active':''}">${v.label}</button>`).join('')}</div></div></div></section>`;}
function flow(m){return `<section class="story-grid" id="why"><article class="panel flow-panel"><div class="panel-head"><div><p class="eyebrow">A system of communicating vessels</p><h2>WARM coordinates every flow.</h2></div><p>Animated direction shows the selected plan.</p></div><div class="flow-map"><div class="node solar"><div class="icon">☀</div><strong>Solar</strong><span>${m.solar} kWh forecast</span></div><div class="node battery"><div class="icon">▣</div><strong>Battery</strong><span>${state.reserve}% reserve</span></div><div class="node home"><div class="icon">⌂</div><strong>Home</strong><span>Evening protected</span></div><div class="node car"><div class="icon">◆</div><strong>Car</strong><span>${m.chargeKwh} kWh needed</span></div><div class="node grid"><div class="icon">↕</div><strong>Grid</strong><span>Peak avoided</span></div><div class="flow-line line-solar pulse"></div><div class="flow-line line-battery pulse"></div><div class="flow-line line-car pulse"></div><div class="flow-line line-grid pulse"></div></div></article><aside class="panel"><div class="panel-head"><div><p class="eyebrow">Explainable reasoning</p><h2>Why this plan?</h2></div></div><div class="reasoning"><article class="reason"><b>1</b><div><strong>Observe</strong><p>${m.solar} kWh solar forecast, €${m.price.toFixed(2)}/kWh evening price and a ${state.reserve}% reserve requirement.</p></div></article><article class="reason"><b>2</b><div><strong>Respect constraints</strong><p>The car must be ready by ${String(state.ready).padStart(2,'0')}:00 and the battery cannot fall below the chosen home reserve.</p></div></article><article class="reason"><b>3</b><div><strong>Reject weaker options</strong><p>Immediate charging increases grid demand; draining the battery leaves the home exposed during the evening.</p></div></article><article class="reason"><b>4</b><div><strong>Recommend</strong><p>Start at ${String(m.chargeStart).padStart(2,'0')}:00. Confidence: ${m.confidence}. Expected value: €${m.saving.toFixed(2)}.</p></div></article></div></aside></section>`;}
function timeline(m){const bars=Array.from({length:24},(_,h)=>{let v=18;if(h>6&&h<19)v+=Math.round(Math.sin((h-6)/12*Math.PI)*65*(m.solar/36));if((m.chargeStart===13&&h>=13&&h<=16)||(m.chargeStart===23&&(h===23||h<4)))v=90;const cls=((m.chargeStart===13&&h>=13&&h<=16)||(m.chargeStart===23&&(h===23||h<4)))?'charge':(h>7&&h<18?'solar':'price');return `<div class="bar ${cls}" style="height:${Math.max(12,v)}px"><span>${h%3===0?String(h).padStart(2,'0'):''}</span></div>`}).join('');return `<section class="panel"><div class="panel-head"><div><p class="eyebrow">24-hour plan</p><h2>Timing becomes visible.</h2></div><p>Gold = solar · green = planned EV charging · grey = other hours</p></div><div class="timeline">${bars}</div><div class="legend"><span><i style="background:#efc45f"></i>solar window</span><span><i style="background:#1f765d"></i>coordinated charging</span><span><i style="background:#cdd8d3"></i>household baseline</span></div></section>`;}
function impact(m){return `<section class="panel" id="impact"><div class="panel-head"><div><p class="eyebrow">Before and after</p><h2>From disconnected devices to one household plan.</h2></div><p>Alternative outcomes are kept separate and transparent.</p></div><div class="compare"><article class="compare-card"><h3>Without coordination</h3><div class="compare-row"><span>EV charging</span><strong>Immediate</strong></div><div class="compare-row"><span>Battery</span><strong>May drain early</strong></div><div class="compare-row"><span>Solar export</span><strong>${Math.round(18-m.solarCapture/3)} kWh</strong></div><div class="compare-row"><span>Decision</span><strong>Four apps</strong></div></article><article class="compare-card good"><h3>With WARM</h3><div class="compare-row"><span>EV charging</span><strong>${String(m.chargeStart).padStart(2,'0')}:00</strong></div><div class="compare-row"><span>Battery</span><strong>${state.reserve}% protected</strong></div><div class="compare-row"><span>Solar captured</span><strong>+${m.solarCapture} kWh</strong></div><div class="compare-row"><span>Decision</span><strong>One plan</strong></div></article></div><div class="impact"><article><strong>€${m.saving.toFixed(2)}</strong><span>daily modeled value</span></article><article><strong>${m.gridPeak} kWh</strong><span>peak import avoided</span></article><article><strong>${Math.round(m.selfUse)}%</strong><span>self-use</span></article><article><strong>${m.confidence}</strong><span>recommendation confidence</span></article></div></section><details class="details"><summary>Evidence, assumptions and safety</summary><div>All household values are synthetic. The demo sends no device commands and stores no changes. The recommendation uses a bounded model combining a 24-hour solar profile, tariff scenario, EV energy need, departure deadline and minimum home-battery reserve. Monetary impact is illustrative, not a quote. Every result is designed to show what information changed the recommendation.</div></details>`;}
function render(){const m=model();document.body.innerHTML=header()+hero(m)+controls()+flow(m)+timeline(m)+impact(m)+`<footer class="footer"><span>WARM · public jury experience</span><span>Read-only · synthetic household · transparent reasoning</span></footer></div>`;bind();}
function bind(){q('#ready').addEventListener('input',e=>{state.ready=Number(e.target.value);render()});q('#reserve').addEventListener('input',e=>{state.reserve=Number(e.target.value);render()});qa('[data-weather]').forEach(b=>b.addEventListener('click',()=>{state.weather=b.dataset.weather;render()}));qa('[data-jump]').forEach(b=>b.addEventListener('click',()=>q('#'+b.dataset.jump).scrollIntoView({behavior:'smooth'})));}
render();